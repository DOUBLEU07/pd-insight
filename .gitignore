# ---------------------------------------------------------------
# Secrets and local configuration
# ---------------------------------------------------------------
# .env holds JWT_SECRET and the database password. Only .env.example
# belongs in the repository.
.env
.env.*
!.env.example
*.pem
*.key

# ---------------------------------------------------------------
# Data: case uploads, databases and dumps
# ---------------------------------------------------------------
# These carry reviewer accounts (bcrypt password hashes) and the PRPD
# images of real measurements, so none of it is publishable.
*.db
*.sqlite
*.sqlite3
*.sql
# Copies taken before a migration carry the same hashes as the original, and
# "*.db" does not match "dev.db.bak".
*.bak
*.dump
*-journal
backend/storage/

# ---------------------------------------------------------------
# Trained models
# ---------------------------------------------------------------
# Mounted in from MODELS_HOST_DIR at runtime; far too large for git.
# Anchored to the repo root on purpose: an unanchored "models/" also matches
# backend/app/models/, which is the ORM package and must be committed.
/models/
*.keras
*.h5

# ---------------------------------------------------------------
# Python
# ---------------------------------------------------------------
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# ---------------------------------------------------------------
# Node and Next.js
# ---------------------------------------------------------------
node_modules/
.next/
out/
build/
*.tsbuildinfo
.eslintcache

# ---------------------------------------------------------------
# Editors and OS
# ---------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
